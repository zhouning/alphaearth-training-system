# Supplementary Material Package

This directory contains reproducibility artifacts for the Paper 12 ISPRS JPRS submission.

## Contents

- `paper12_results/` - BigEarthNet, LandCover.ai, LoRA ablation, full-finetuning, rank-sensitivity, LoveDA two-direction full-finetuning, EuroSAT channel-bridge, and SatMAE-compatible second-backbone result files.
- `results/` - EuroSAT and GeoAdapter result JSON files copied from the AlphaEarth-System working tree.
- `scripts/` - figure-generation and diagnostic scripts copied from the manuscript source tree.

Note: `paper12_results/eurosat_channel_bridge*.json` is the rerun output after the checkpoint-path fix and can be cited as manuscript evidence once the package is uploaded.

Completed: `paper12_results/peft_capacity_sweep.json` and `paper12_results/peft_capacity_sweep_summary.json` are mirrored from `colab/paper12_peft_capacity_sweep_colab.ipynb` and are manuscript-ready once the package is uploaded.

Completed: `paper12_results/second_backbone_eurosat.json` and `paper12_results/second_backbone_eurosat_summary.json` are mirrored from `colab/paper12_second_backbone_eurosat_colab.ipynb` and support the bounded SatMAE-compatible second-backbone validation.

Completed: `paper12_results/review_audit_summary.json` is a deterministic derived audit generated by `python -m geoadapter.bench.paper12_audit`; it does not add training runs, but records the exact capacity, channel-bridge, second-backbone, LoveDA diagnostic, model-scope, label-source, decoder-capacity, and ArcGIS replacement-boundary checks cited in the revised manuscript.

Boundary file: `paper12_results/arcgis_replacement_validation_template.json` records that Paper12 is not a validated ArcGIS replacement until independent manual labels, ArcGIS reference outputs, Paper12 checkpoint-backed outputs, and paired metrics are available for the same area, time window, and class taxonomy. The referenced validation workflow scripts (`prepare_arcgis_replacement_validation_packet.py`, `export_paper12_packet_predictions.py`, `finalize_arcgis_replacement_validation_packet.py`, and `evaluate_arcgis_replacement.py`) live under the main repository `scripts/` directory; this supplementary package mirrors the conservative protocol and result artifacts.

## Recommended Supplementary Archive

Zip this directory after confirming that all files are allowed to be shared:

```powershell
Compress-Archive -Path .\06_supplementary_material\* -DestinationPath .\paper12_isprs_jprs_supplementary.zip -Force
```

## Checks Before Upload

- Confirm that all result files cited in the manuscript are present.
- If the PEFT capacity sweep is cited, confirm that `peft_capacity_sweep.json` and `peft_capacity_sweep_summary.json` are present and synchronized with the manuscript tables.
- Confirm that `loveda_full_finetune_u2r.json`, `loveda_full_finetune_r2u.json`, and `loveda_full_finetune_summary.json` remain synchronized before citing the two-direction LoveDA full fine-tuning baseline.
- Confirm that the EuroSAT channel-bridge rerun JSON and summary are mirrored into the supplementary package before upload.
- Confirm that the second-backbone EuroSAT JSON and summary are mirrored into the supplementary package before upload.
- Confirm that any Linhe data-sharing restrictions are documented in the data availability statement.
- Confirm that `review_audit_summary.json` remains schema version 2 and includes model-scope, second-backbone, label-source, decoder-capacity, and ArcGIS replacement-boundary checks before upload.
- Confirm that `arcgis_replacement_validation_template.json` is mirrored and still records `not_validated` unless independent replacement evidence has been added.
- Confirm that logs do not expose local machine paths, private tokens, or restricted dataset locations.

